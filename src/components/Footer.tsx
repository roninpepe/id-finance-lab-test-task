import React, { FC } from 'react';
import styles from 'styles/components/Footer.module.scss';

const Footer: FC = () => {
  return <footer className={styles._}>© 2022</footer>;
};

export default Footer;
